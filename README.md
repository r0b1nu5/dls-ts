# Applied Statistics: Time Series

Teaching material for the course Applied Statistics: Time Series.

The rendered book and slides are published at <https://dls.hevs.ch/ts/> by the
GitHub Actions workflow in `.github/workflows/render.yml` on every push to `main`.
The workflow needs the `API_TOKEN` repository secret for the upload to dls.hevs.ch.
